{
  "name": "hugo-site",
  "version": "1.0.0",
  "scripts": {
    "dev": "hugo server --bind 0.0.0.0 --port 1313",
    "build": "hugo --minify -d public",
    "start": "hugo server",
    "resize:once": "node scripts/resize-uploads.js --once",
    "resize:watch": "node scripts/resize-uploads.js"
  },
  "dependencies": {
    "chokidar": "^5.0.0",
    "sharp": "^0.34.5"
  }
}
