{
  "name": "hugo-site",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx tinacms dev --port 4002 --command \"hugo server --bind 0.0.0.0 --port 1313\"",
    "build": "npx tinacms build",
    "start": "hugo server"
  },
  "dependencies": {
    "@tinacms/cli": "^2.0.4",
    "tinacms": "^3.1.1"
  }
}
